{
  "name": "agent-julia",
  "version": "0.1.9",
  "description": "One brain for your AI — a local-first, owned memory and configurable persona shared across Claude Code, Cowork, and Dispatch, exposed as an MCP server.",
  "author": "Paweł Gniadkowski (elniño) <el@elnino.pl>",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agent-julia": "dist/index.js"
  },
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/elninopl/agent-julia.git"
  },
  "bugs": {
    "url": "https://github.com/elninopl/agent-julia/issues"
  },
  "homepage": "https://github.com/elninopl/agent-julia#readme",
  "files": [
    "dist",
    "assets",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && npm run copy:assets",
    "copy:assets": "node scripts/copy-assets.mjs",
    "dev": "tsx src/index.ts",
    "wizard": "tsx src/index.ts init",
    "serve": "tsx src/index.ts serve",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.8.0",
    "franc-min": "^6.2.0",
    "gray-matter": "^4.0.3",
    "zod": "^3.24.0"
  },
  "peerDependencies": {
    "@huggingface/transformers": "^3 || ^4"
  },
  "peerDependenciesMeta": {
    "@huggingface/transformers": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "keywords": [
    "mcp",
    "memory",
    "persona",
    "claude",
    "claude-code",
    "local-first",
    "knowledge-base"
  ]
}
